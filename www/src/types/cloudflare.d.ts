// Committed so `tsc` knows about D1Database and friends without first running
// `npm run cf-typegen` (whose output, cloudflare-env.d.ts, is gitignored).
/// <reference types="@cloudflare/workers-types" />
